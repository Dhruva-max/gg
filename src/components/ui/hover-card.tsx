"use client";
import React from "react";

export default function hover-card(props: any) {
  return (
    <div style={{ padding:12, border:'1px dashed #ccc' }}>
      <strong>Placeholder:</strong> hover-card — original file was corrupted and replaced.
    </div>
  );
}
